[package]
name = "virtio"
version = "0.1.0"
edition = "2024"
license.workspace = true

[dependencies]
abi = { version = "0.1.0", path = "../abi" }
fs_proto = { version = "0.1.0", path = "../fs_proto" }
user_rt = { version = "0.1.0", path = "../user_rt" }
