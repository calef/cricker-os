--- a/Cargo.toml
+++ b/Cargo.toml
@@ -8,6 +8,12 @@
 # If you are reading this file be aware that the original Cargo.toml
 # will likely look very different (and much more reasonable).
 # See Cargo.toml.orig for the original contents.
+
+# cricker-os pin divergence: an empty [workspace] table marks this vendored crate as its own
+# workspace root, so the outer cricker-os workspace treats it as an external path dependency, not a
+# member. That is what keeps our `-D warnings` clippy gate and our `cargo fmt` gate off code we do
+# not own, while it still builds normally as redoxfs_host's dependency. See vendor/README.md.
+[workspace]
 
 [package]
 edition = "2021"
--- a/src/filesystem.rs
+++ b/src/filesystem.rs
@@ -2,6 +2,7 @@
 use alloc::{
     collections::{BTreeMap, VecDeque},
     vec,
+    vec::Vec, // cricker-os pin divergence: patches/redoxfs-no-std-vec-import.patch
 };
 use syscall::error::{Error, Result, EKEYREJECTED, ENOENT, ENOKEY};
 use xts_mode::{get_tweak_default, Xts128};
--- a/src/record.rs
+++ b/src/record.rs
@@ -1,3 +1,4 @@
+use alloc::vec::Vec; // cricker-os pin divergence: patches/redoxfs-no-std-vec-import.patch
 use alloc::vec;
 use core::ops;
 
